#include <iostream>
#include <vector>

using namespace std;
int main(){
    int N;
    int previous = 0;
    cin >> N;
    vector<int> knights(N);
    vector<int> knightsquads(N);
    for(int i = 0; i<N; i++) cin >> knights[i];
    
    return 0;
}